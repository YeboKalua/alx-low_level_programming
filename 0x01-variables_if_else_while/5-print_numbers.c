#include <stdio.h>
/**
 * main - main function
 * description: print numbers from 0 to 9
 * Return: 0 if successful
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
